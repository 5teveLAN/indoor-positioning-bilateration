# 3.1.1 測試模式

> 來源檔：
> - `app/app/src/main/java/com/mcu/bluetooth/RoleSelectionActivity.kt`（登入 / 角色分流）
> - `app/app/src/main/java/com/mcu/bluetooth/MainActivity.kt`（學生端廣播、老師端容器）
> - `app/app/src/main/java/com/mcu/bluetooth/TeacherControlsFragment.kt`（老師端掃描 / 解密 / 回傳）
> - `app/app/src/main/java/com/mcu/bluetooth/NetworkManager.kt`（HTTP API）
>
> 對照：`APP端如何處理封包.md`（BLE 封包格式）、`3.1.2 我自己寫的測試模式.md`（測試帳號 OTP 對照表）

---

## 0. 兩者的差異只有「入口」

自動點名之後的流程（要 OTP / XOR Key → XOR 加密 → BLE 廣播 → 老師端解密比對 → 回傳伺服器）**兩種模式完全一樣**，程式碼是同一份，沒有任何 `if (測試模式)` 分支。

唯一差別在 **`RoleSelectionActivity` 怎麼把角色與 email 交給 `MainActivity`**：

| 項目 | 普通模式 | 測試模式 |
|------|---------|---------|
| 進入方式 | 輸入帳號密碼 → 按「登入」 | 按「**以老師／學生身分測試**」→ 免登入 |
| 有無打後端 | ✅ `POST /api/auth/login`（`email` + `password` + `device_id`） | ❌ 完全不打 API，直接跳頁 |
| 角色怎麼決定 | 由 email `@` 前是否全為數字推斷 | 由按鈕直接寫死 |
| 帶入的 EXTRA_EMAIL | 使用者輸入的真實帳號 | 老師 `test_teacher@mail.mcu.edu.tw`<br>學生 `11012345@me.mcu.edu.tw` |
| 推導出的學號 | 真實學號（`email.substringBefore("@")`） | 固定 `11012345` |
| 登入狀態 | 寫入 SharedPreferences（`saved_email`／`saved_role`），下次自動進站 | 不寫入，**每次都要重按** |
| 換場地 / 換人 | 需重新登入 | 只要改按鈕寫死的 email（或改用 3.1.2 的多組學號選擇） |
| 裝置綁定檢查 | ✅ 後端驗 `device_id` | ❌ 跳過（老師端可用模擬器） |

> 測試模式的 UI 位置：`activity_role_selection.xml` 的 `test_label`
> 「`--- 快速測試進入 (無需登入) ---`」底下兩個 `OutlinedButton`：
> `teacher_button`（以老師身分測試）、`student_button`（以學生身分測試）。


---

## 1. 普通模式（正式模式）流程圖

```mermaid
flowchart TD
    A([開啟 App<br>RoleSelectionActivity.onCreate]) --> B{SharedPreferences<br>有 saved_email<br>且 saved_role？}

    B -- 有 --> C["startMainActivity(saved_role, saved_email)<br>（自動進站，跳過登入畫面）"]
    B -- 沒有 --> D["setContentView<br>顯示登入卡片"]

    D --> E["輸入帳號 + 密碼<br>按「登入」"]
    E --> F{"帳號或密碼<br>為空？"}
    F -- 是 --> G["Toast：請輸入帳號與密碼"] --> E

    F -- 否 --> H["formatEmail(input)<br>8 位純數字 → @me.mcu.edu.tw<br>其他 → @mail.mcu.edu.tw"]
    H --> I["getUniqueDeviceId()<br>Settings.Secure.ANDROID_ID"]
    I --> J["NetworkManager.login<br>POST /api/auth/login<br>{email, password, device_id}"]

    J --> K{"後端回應<br>success / token /<br>status=success？"}
    K -- 否 --> L["Toast：登入失敗<br>（例如：設備未綁定）"] --> E

    K -- 是 --> M["role = if (localPart 全為數字)<br>STUDENT else TEACHER"]
    M --> N["寫入 SharedPreferences<br>saved_email / saved_role"]
    N --> C

    C --> O["MainActivity.onCreate<br>讀 EXTRA_ROLE / EXTRA_EMAIL<br>studentId = email.substringBefore('@')"]
    O --> P{"currentRole"}
    P -- STUDENT --> Q["顯示學生卡<br>學號 = studentId"]
    P -- TEACHER --> R["顯示 ViewPager2<br>TeacherControlsFragment + HeatmapFragment"]
    P -- 其他 --> S["回首頁 RoleSelectionActivity<br>finish()"]

    Q --> T(["進入背景自動點名<br>（見 §3）"])
    R --> U(["進入老師端掃描點名<br>（見 §4）"])

    style A fill:#e3f2fd,stroke:#1565c0
    style T fill:#e8f5e9,stroke:#2e7d32
    style U fill:#e8f5e9,stroke:#2e7d32
    style J fill:#fff3e0,stroke:#ef6c00
    style K fill:#fff3e0,stroke:#ef6c00
```

---

## 2. 測試模式流程圖

```mermaid
flowchart TD
    A([開啟 App<br>RoleSelectionActivity.onCreate]) --> B{SharedPreferences<br>有 saved_email<br>且 saved_role？}

    B -- 有 --> C["startMainActivity(saved_role, saved_email)<br>⚠️ 之前登入過就會自動進站<br>碰不到測試按鈕"]
    B -- 沒有 --> D["顯示登入卡片<br>+「快速測試進入（無需登入）」"]

    D --> E{"按哪個測試按鈕？"}

    E -- "以老師身分測試<br>teacher_button" --> F["startMainActivity(<br>TEACHER,<br>test_teacher@mail.mcu.edu.tw)"]
    E -- "以學生身分測試<br>student_button" --> G["startMainActivity(<br>STUDENT,<br>11012345@me.mcu.edu.tw)"]

    F --> H["MainActivity.onCreate<br>studentId = test_teacher<br>（老師端不廣播，學號不影響）"]
    G --> H

    H --> I{"currentRole"}
    I -- STUDENT --> J["顯示學生卡<br>學號 = 11012345"]
    I -- TEACHER --> K["顯示 ViewPager2<br>TeacherControlsFragment + HeatmapFragment"]

    J --> L(["立即啟動自動點名<br>handler.post(autoAttendanceRunnable)<br>（見 §3）"])
    K --> M(["老師端手動按「開始點名」<br>（見 §4）"])

    N[/"❌ 沒有打的 API：<br>/api/auth/login（登入）<br>/api/register（註冊）<br>/api/send-code（驗證碼）"/]

    L -.-> N
    M -.-> N

    style A fill:#e3f2fd,stroke:#1565c0
    style E fill:#f3e5f5,stroke:#6a1b9a
    style F fill:#f3e5f5,stroke:#6a1b9a
    style G fill:#f3e5f5,stroke:#6a1b9a
    style C fill:#ffe0e0,stroke:#c62828
    style N fill:#ffe0e0,stroke:#c62828
    style L fill:#e8f5e9,stroke:#2e7d32
    style M fill:#e8f5e9,stroke:#2e7d32
```

> ⚠️ **`B -- 有` 這條路是測試模式的陷阱**：只要普通模式登入過一次，
> SharedPreferences 就會一直被帶入，測試按鈕永遠看不到。
> 需要先按「設定 → 登出」（`logout()` 會 `sharedPref.edit().clear()`）才會回到登入頁。

---

## 3. 學生端自動點名（兩種模式共用）

```mermaid
flowchart TD
    A(["MainActivity.onCreate<br>currentRole == STUDENT"]) --> B["checkAndStartAutoAttendance()"]

    B --> C{"已取得<br>BLE 權限？"}
    C -- 否 --> D["requestMultiplePermissions<br>BLUETOOTH_SCAN / ADVERTISE / CONNECT<br>（API &lt; 31：BLUETOOTH / BLUETOOTH_ADMIN / ACCESS_FINE_LOCATION）"]
    D --> E{"全部允許？"}
    E -- 否 --> F["Toast：未取得權限，自動點名無法運作"] --> Z([結束])
    E -- 是 --> G
    C -- 是 --> G["handler.post(autoAttendanceRunnable)<br>→ startAutomaticAttendance()"]

    G --> H{"isMultipleAdvertisementSupported<br>== false？"}
    H -- 是 --> I["Toast：您的手機不支援 BLE 廣播功能"] --> Z
    H -- 否 --> J{"studentId 為空<br>或 == 'Unknown'？"}
    J -- 是 --> Z

    J -- 否 --> K["status = 正在同步權杖 (HH:mm:ss)"]
    K --> L["NetworkManager.getStudentToken(studentId)<br>POST /api/session/get-token<br>{student_id}"]

    L --> M{"拿到 otp 與 xorKey？"}
    M -- 否 --> N["status：目前無點名活動<br>stopBleAdvertising()"] --> Y

    M -- 是 --> O["broadcastEncryptedMessage(id, otp, xorKey)"]
    O --> P["rawData = '學號|OTP'<br>rawBytes = UTF-8 bytes"]
    P --> Q["encrypted[i] = rawBytes[i] XOR keyBytes[i % keyLen]"]
    Q --> R{"encrypted.size > 26？"}
    R -- 是 --> S["直接 return（不廣播）"] --> Y

    R -- 否 --> T["先 stopBleAdvertising() 停掉舊的"]
    T --> U["AdvertiseSettings<br>LOW_LATENCY / connectable = false"]
    U --> V["AdvertiseData<br>+ Service UUID 0x1111<br>+ Service Data = encrypted bytes"]
    V --> W["bleAdvertiser.startAdvertising()"]
    W --> X["status：自動發送中 (OTP: xxx)"] --> Y

    Y["handler.postDelayed(autoAttendanceRunnable, 2 分鐘)"]
    Y --> G

    style A fill:#e3f2fd,stroke:#1565c0
    style L fill:#fff3e0,stroke:#ef6c00
    style M fill:#fff3e0,stroke:#ef6c00
    style W fill:#e8f5e9,stroke:#2e7d32
    style S fill:#ffe0e0,stroke:#c62828
```

**重點**

- 間隔 `AUTO_REFRESH_INTERVAL = 2 * 60 * 1000L`：學生端每 **2 分鐘**重新拿一次 token 並重新廣播。
  刻意比老師端的 5 分鐘短，確保老師換 Key 時學生能更快跟上。
- 手動按鈕「手動立即簽到」也是呼叫同一個 `startAutomaticAttendance()`。
- 權限被拒後**不會**再排程，必須重開 App。

---

## 4. 老師端掃描點名（兩種模式共用）

```mermaid
flowchart TD
    A(["TeacherControlsFragment<br>進入頁面"]) --> B["btnAttendanceToggle<br>文字「開始點名」"]

    B --> C{"按下按鈕"}
    C -- "isScanning == false" --> D["checkAndRequestPermissions()"]
    C -- "isScanning == true" --> Z1["stopAttendanceAndUpload()（見 §5）"]

    D --> E{"BLE 權限齊全？"}
    E -- 否 --> F["requestPermissionsLauncher.launch()"]
    F --> G{"使用者同意？"}
    G -- 否 --> H["Toast：請授權權限以開始點名"] --> B
    G -- 是 --> I
    E -- 是 --> I["startSecureSessionLoop()"]

    I --> J{"藍牙已開啟？"}
    J -- 否 --> K["Toast：請先開啟藍牙"] --> B

    J -- 是 --> L["isScanning = true<br>按鈕變紅「停止並回傳」<br>清空 attendanceResults / attendanceRecords<br>所有學生 isPresent = false"]
    L --> M["performSessionRefresh()"]
    M --> N["email = intent EXTRA_EMAIL<br>（測試模式 = test_teacher@mail.mcu.edu.tw）"]
    N --> O["NetworkManager.startAttendanceSession(email)<br>POST /api/session/start<br>{email}"]

    O --> P{"回傳 xor_key？"}
    P -- 否 --> Q["status 停在「正在同步伺服器點名訊號...」<br>（沒有 key，掃到封包也解不開）"] --> B
    P -- 是 --> R["currentXorKey = xor_key"]
    R --> S["NetworkManager.getVerifyList(email)<br>POST /api/session/otp-list<br>{email}"]
    S --> T["otpVerifyList = 學號→OTP 對照表<br>updateGridFromOtpList()<br>建立學生格狀清單"]
    T --> U["status：✅ 點名循環中 (HH:mm)<br>handler.postDelayed(refreshRunnable, 5 分鐘)"]
    U --> V["ScanFilter.setServiceData(UUID 0x1111)<br>ScanSettings.SCAN_MODE_LOW_LATENCY<br>bleScanner.startScan()"]

    V --> W(["持續接收 BLE 廣播<br>（見 §4.1 解密流程）"])
    W -.->|"每 5 分鐘"| M

    style A fill:#e3f2fd,stroke:#1565c0
    style O fill:#fff3e0,stroke:#ef6c00
    style S fill:#fff3e0,stroke:#ef6c00
    style V fill:#e8f5e9,stroke:#2e7d32
```

### 4.1 `scanCallback` 解密與驗證

```mermaid
flowchart TD
    A(["onScanResult"]) --> B["payload = scanRecord.getServiceData(UUID 0x1111)"]
    B --> C{"payload 為 null？"}
    C -- 是 --> Z([忽略])
    C -- 否 --> D["address = result.device.address"]
    D --> E{"currentXorKey 為 null？"}
    E -- 是 --> Z
    E -- 否 --> F["decrypted[i] = payload[i] XOR keyBytes[i % keyLen]<br>decryptedStr = UTF-8 解碼"]

    F --> G{"含 '|' 分隔符？"}
    G -- 否 --> Z
    G -- 是 --> H["parts = decryptedStr.split('|')<br>studentId = parts[0]<br>receivedOtp = parts[1]"]

    H --> I["expectedOtp = otpVerifyList[studentId]"]
    I --> J{"expectedOtp != null<br>且 receivedOtp == expectedOtp？"}
    J -- 否 --> Z

    J -- 是 --> K["processCheckInResult(studentId, address)"]
    K --> L["attendanceResults[address] =<br>'[學號] ✅ 點名成功 (HH:mm:ss)'<br>attendanceRecords[address] = Pair(學號, 時間)"]
    L --> M["updateListView() 更新即時清單"]
    M --> N["該學生 isPresent = true<br>gridAdapter.notifyItemChanged()<br>updateAttendanceSummary()"]

    style A fill:#e3f2fd,stroke:#1565c0
    style J fill:#f3e5f5,stroke:#6a1b9a
    style K fill:#e8f5e9,stroke:#2e7d32
    style N fill:#e8f5e9,stroke:#2e7d32
```

**關鍵：Key 必須同一把**

學生端加密用的 `xorKey`（`/api/session/get-token` 拿到的）與老師端解密用的 `currentXorKey`
（`/api/session/start` 拿到的）**必須是同一場 session 的同一把 key**，否則解出來是亂碼，
`split("|")` 後的學號／OTP 都不會對，`otpVerifyList` 比對失敗 → 直接丟棄。

---

## 5. 結束點名與回傳

```mermaid
flowchart TD
    A(["按下「停止並回傳」"]) --> B["isScanning = false<br>handler.removeCallbacks(refreshRunnable)<br>bleScanner.stopScan()"]
    B --> C["按鈕變綠「開始點名」<br>status：正在回傳點名結果..."]

    C --> D["把「手動勾選但沒被 BLE 感應到」的學生<br>補進 attendanceRecords<br>key = 'Manual_學號'"]

    D --> E{"attendanceRecords 為空？"}
    E -- 是 --> F["status：點名結束 (無紀錄)"] --> Z([結束])

    E -- 否 --> G["AtomicInteger 計數，逐筆<br>NetworkManager.syncAttendance(學號, address)<br>POST /api/check-in<br>{student_info, device_address}"]
    G --> H["全部完成後<br>status：點名結束，已同步 N 位學生<br>Toast：點名名單回傳完成"]
    H --> Z

    Z --> I["其他操作<br>• 導出 CSV → 下載資料夾 點名結果_yyyyMMdd_HHmm.csv<br>• 設定 → 登出 → clear SharedPreferences → 回首頁"]

    style A fill:#e3f2fd,stroke:#1565c0
    style G fill:#fff3e0,stroke:#ef6c00
    style I fill:#f3e5f5,stroke:#6a1b9a
```

> ⚠️ **注意**：`syncAttendance` 的第二個參數是 BLE 的 `device.address`（MAC），
> 但手機 MAC 會隨機化（LE Random Address）而變動；
> 與 `indoor-locator/README.md`「身分識別 = `student_id`，不使用 BLE MAC」的設計不一致。

---

## 6. 兩模式並存的全域對照圖

```mermaid
flowchart LR
    subgraph ENTRY["入口（唯一差異處）"]
        direction TB
        N1["普通模式<br>輸入帳密 → 登入"]
        N2["測試模式<br>快速測試按鈕<br>（免登入）"]
    end

    N1 --> API1["POST /api/auth/login<br>（帳密 + device_id）"]
    API1 --> SP["SharedPreferences<br>saved_email / saved_role"]
    N2 -.->|"跳過"| SP
    SP --> MA["MainActivity<br>EXTRA_ROLE / EXTRA_EMAIL"]

    MA --> ST{"角色"}
    ST -- STUDENT --> STU["學生端<br>每 2 分鐘廣播"]
    ST -- TEACHER --> TEA["老師端<br>每 5 分鐘換 Key"]

    STU --> SHARED
    TEA --> SHARED

    subgraph SHARED["共用管線（兩模式完全相同）"]
        direction TB
        K1["POST /api/session/get-token<br>→ otp + xor_key"]
        K2["POST /api/session/start<br>→ xor_key"]
        K3["POST /api/session/otp-list<br>→ 學號→OTP"]
        BLE["BLE Advertisement<br>UUID 0x1111<br>Service Data = XOR('學號|OTP')"]
        CK["解密比對 → processCheckInResult"]
        UP["POST /api/check-in"]
    end

    K1 --> BLE
    K2 --> CK
    K3 --> CK
    BLE --> CK
    CK --> UP

    style ENTRY fill:#f3e5f5,stroke:#6a1b9a
    style SHARED fill:#e8f5e9,stroke:#2e7d32
    style API1 fill:#fff3e0,stroke:#ef6c00
    style N2 fill:#e3f2fd,stroke:#1565c0
```

---

## 7. 目前程式碼的實作狀態（誠實對照）

| 文件寫的 | 程式碼實際狀況 |
|---------|--------------|
| 測試模式用固定 `TEST_XOR_KEY = "TEST_KEY_2024"` | ❌ **App 尚未實作**。App 端沒有 `USE_TEST_MODE` / `TEST_KEY_2024` 常數，測試模式的學生端一樣打 `/api/session/get-token`。 |
| 免登入測試模式 | ✅ 已實作，但只做到「跳過登入」，**OTP 與 XOR Key 仍需要伺服器**（見 `TODO.md`）。 |
| 測試用固定 OTP 對照表 | ❌ 未在 App 內。固定學號 `11012345`，OTP 由後端給；`3.1.2 我自己寫的測試模式.md` 的 `A1B2C3`／`D4E5F6` 等是給模擬器（`mock_sensor.py --otp-list`）用的。 |
| 第一階段臨時伺服器免登入 | ✅ 但 `mock_server.py` 的 `new_xor_key()` 是**隨機產生**，不是寫死 `TEST_KEY_2024`（刻意與第二/三階段行為一致）。 |
| 老師端用模擬器 run | ⚠️ 需 `isMultipleAdvertisementSupported` 為 true 的裝置才能廣播；老師端只掃描所以模擬器可跑，但模擬器常回無 BLE 適配器。 |

### API 使用狀況

| 函式 | 端點 | 狀態 |
|------|------|------|
| `login` | `POST /api/auth/login` | 測試模式不使用 |
| `registerUser` | `POST /api/register` | 測試模式不使用 |
| `requestVerifyCode` | `POST /api/send-code` | 測試模式不使用 |
| `startAttendanceSession` | `POST /api/session/start` | ✅ 老師端每次刷新都打 |
| `getVerifyList` | `POST /api/session/otp-list` | ✅ 老師端每次刷新都打 |
| `getStudentToken` | `POST /api/session/get-token` | ✅ 學生端每 2 分鐘打 |
| `syncAttendance` | `POST /api/check-in` | ✅ 停止點名時逐筆打 |
| — | `POST /api/coords/get` | ❌ App 端還沒接（`HeatmapFragment` 目前跑 `simulateServerData()` 假資料） |
| — | `POST /api/coords/clear` | ❌ App 端還沒接 |

`NetworkManager.BASE_URL = "https://micronemous-indefeasibly-cooper.ngrok-free.dev"`（寫死在程式碼中）。

---

## 8. 測試模式操作前提（速記）

1. **先確定沒有殘留登入**：若 App 直接跳過登入頁，按「設定（齒輪）→ 登出」清掉 SharedPreferences。
2. **老師端**：按「以老師身分測試」→ `TeacherControlsFragment` → 按「開始點名」。
   後端必須已經有 `test_teacher@mail.mcu.edu.tw` 的 session（`mock_server.py` / Node 需接受這個 email）。
3. **學生端**：按「以學生身分測試」→ 自動以學號 `11012345` 開始每 2 分鐘廣播。
   老師端的 `otp_list` 必須含 `11012345`，否則解密後 `expectedOtp == null` → 丟棄。
4. **學生端必須實機**：需要 `isMultipleAdvertisementSupported == true`，模擬器通常不支援 BLE 廣播。
5. **兩端 Key 必須同場**：先讓老師端「開始點名」拿到 key 並啟動掃描，再讓學生端廣播。




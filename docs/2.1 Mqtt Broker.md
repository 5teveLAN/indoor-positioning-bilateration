1. 要能對外連線 (測試時可用內網ip，上線時採用ngrok做暫時測試？)
2. 連線必須密碼驗證
3. 傳輸必須加密
	- 要用 websocket，beacon 需要改 code，甚至要換 library

### 加密
- 有兩種 MQTT: MQTT over TCP(1883) MQTT over websocket(9001)
- 各自有各的加密協定 MQTTs(8883),  Mqtt over wss(8083)
- iot 裝置比較支援 1883